# SpaceXAI (xAI) — never commit the real key
XAI_API_KEY=

# OpenAI-compatible base URL and model (swap provider by changing these)
LLM_BASE_URL=https://api.x.ai/v1
LLM_MODEL=grok-4.6

# Local test server. 0.0.0.0 so Windows Chrome can reach a WSL server via localhost.
HOST=0.0.0.0
PORT=8000
